import { StyleSheet } from "react-native";

const Styles = StyleSheet.create({
    form:{
        height: 40, 
        borderColor: 'gray', 
        borderWidth: 1, 
        marginBottom: 10
    }
})

export default Styles